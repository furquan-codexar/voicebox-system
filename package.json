{
  "name": "voicebox",
  "version": "0.1.12",
  "private": true,
  "scripts": {
    "dev:server": "uvicorn backend.main:app --reload --port 17493",
    "build:server": "cd backend && python build_binary.py"
  }
}
